# Bright Data pending job

Check the persisted external job ID and poll budget. Do not start a duplicate collection. After the bounded retry window, mark the run pending for operator review while continuing to serve last-known-good facts.
